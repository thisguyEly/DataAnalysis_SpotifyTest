library(tidyverse)
library(datasets)
library(dplyr)


# displays the column names
names(iris)

# shows the first 6 rows
#head()
colnames()
# shows the last 6 rows
tail(iris)

#iris |>
 # filter(Sepal.Length > 5.0) |>
  #View()

